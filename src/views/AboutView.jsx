import React from 'react';
import Header from '../components/common/Header';

const AboutView = () => {
  return (
    <div>
      <Header text={`How to use Solo No Mo'`} size={'2xl'} />
    </div>
  );
};

export default AboutView;
